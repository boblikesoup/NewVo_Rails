<!--
<div class='nv-block'><% if signed_in? %>
  <div class='nv-left'><%= image_tag current_user.profile_pic %> Welcome <%= current_user.first_name %>!</div>
  <div class='nv-mid'>NewVo</div>
  <div class='nv-right-top-menu'><%= link_to "Sign out", signout_path %></div>
</div>
 -->

<%=  render 'post_form' %>

<% if @posts %>
  <% @posts.each do |post| %>
    <% if post.published == true %>
    <%= render partial: 'post', object: post %>
    <% end %>
  <% end %>
<% end %>
